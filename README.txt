A shell script to extract content from wex files.
Known problems: index.html lacks valid references to images:
<img src="file_name.extension" />
Images are extracted in wrong order.

Dependencies:
bash zip p7zip-full file findutils grep coreutils

